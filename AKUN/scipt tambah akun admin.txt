node -e "
const { PrismaClient } = require('@prisma/client');
const bcrypt = require('bcryptjs');
const prisma = new PrismaClient();
(async () => {
  const name = 'Admin Baru';
  const username = 'admin1';
  const email = 'admin1@xyz.com';
  const password = await bcrypt.hash('admin1', 12);
  await prisma.user.create({ data: { name, username, email, password, role: 'ADMIN' } });
  console.log('Admin baru berhasil dibuat!');
})();
"